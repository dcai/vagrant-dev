vm:
  shares:
    # code:
    #   host_dir: './code'
    #   vm_mount_point: '/var/www'
    #   type: 'nfs_guest'
    vagrant:
      host_dir: "."
      vm_mount_point: "/vagrant"
      owner: "{{ home_username }}"
      group: "{{ home_username }}"
      type: ""
    dropbox:
      owner: "{{ home_username }}"
      group: "{{ home_username }}"
      host_dir: "../Dropbox/"
      vm_mount_point: "/home/ubuntu/Dropbox"
      type: ""
